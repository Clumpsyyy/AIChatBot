# AIChatBot

This project implements a Retrieval-Augmented Generation (RAG) pipeline that transforms structured JSON data into vector embeddings and enables context-aware question answering using locally hosted Large Language Models (LLMs) powered by Ollama.
